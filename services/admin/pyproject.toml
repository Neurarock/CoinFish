# Admin / vault ops microservice — solvency, fees, grace loans, operator tools.

[project]
name = "coinfish-admin"
version = "0.1.0"
description = "CoinFish admin/vault microservice"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pydantic>=2.0",
]

[tool.uv]
package = false
