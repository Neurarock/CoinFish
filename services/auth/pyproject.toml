# Auth / identity microservice — JWT/session + KYC hooks (extracted from product API over time).

[project]
name = "coinfish-auth"
version = "0.1.0"
description = "CoinFish auth microservice"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pydantic>=2.0",
]

[tool.uv]
package = false
