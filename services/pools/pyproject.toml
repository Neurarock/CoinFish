# Pools microservice — risk tiers, capacity, utilisation.

[project]
name = "coinfish-pools"
version = "0.1.0"
description = "CoinFish pools microservice"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pydantic>=2.0",
]

[tool.uv]
package = false
